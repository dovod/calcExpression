calc_expressions.exe - консольное приложение для расчёта арифметических выражений.

1. При запуске появляется приглашение для ввода выражения(Enter the expression:).
В строку под приглашением нужно ввести выражение: (12 + х) * 23 + y
	В ответ на ввод корректного выражения вернется: Ok

2.Если выражение содержит переменные, то нужо будет поледовательно ввести их в формате:
x=10
	В ответ на ввод корректного выражения вернется: Ok
	и текущее состояние всех переменных
Current state of variables: x=10, y=null

3. Команда calc произведет вычисление выражение и вернет результат

4. Команда print выведет json структуру синтактического дерева
	json не отформатированный!
print
{"value":0,"operation":"ADD","leftNode":{"value":1,"operation":null,"leftNode":null,"rightNode":null},"rightNode":{"value":3,"operation":null,"leftNode":null,"rightNode":null}}

